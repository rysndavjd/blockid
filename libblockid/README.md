`libblockid` is superblock detection library supporting both `std` and `no_std`
environments. It requires only a memory allocator, with optional support 
for operating system dependencies via `os_calls` for enhanced superblock
detection and topology infomation provided by the operating system.