`libblockid_core` is a portable superblock detection library supporting both 
`std` and `no_std` environments. It requires only a memory allocator, with no
kernel dependencies, making it suitable for embedded and bare-metal targets.